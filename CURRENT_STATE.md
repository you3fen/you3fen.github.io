# 当前实现：三分 · 私人陈列

更新日期：2026-09-05。本文描述当前代码，替代此前混有多轮历史状态的记录。旧记录可从 Git 历史查询。

## 网站与发布路径

- 正式域名：`https://yousanfen.com/`。
- 用户仓库：`https://github.com/you3fen/you3fen.github.io`；日常只发布到 `origin/main`。
- GitHub Pages 工作流：`.github/workflows/pages.yml`。
- 输出目录：`public/`；域名文件必须是输出根目录的普通文件 `CNAME`。
- 线上版本以该仓库 main 最近成功的 Pages 工作流为准。

## 当前体验

- 原创「私人陈列」首页，延续用户认可样稿的宋体标题、留白、透明展台和物件照片质感。
- `/objects/running/`：两次半程马拉松与 1:48:37 记录。
- `/objects/bench-scout/`：参考内容进入飞书表格的工具及制作经历。
- `/objects/unfinished/`：TextLens 的尝试、停止原因与留下的经验。
- `/about/`：关于三分、概念图说明、源码及旧桌面入口。
- `/404.html`：错误网址的返回入口。
- `/inner/`：旧版桌面，文件原样保留。

新站使用原生多页链接。刷新、独立访问、键盘和浏览器历史行为由浏览器处理，不用自建前端路由。JavaScript 仅控制明暗模式，偏好键为 `sanfen-collection-theme`；无保存值时跟随系统。存储不可用时仍允许当页切换，关闭脚本时正文与导航仍可用。

## 构建与资源

保留 npm、Webpack 和现有锁文件，不新增运行依赖。新入口为 `src/collection.js`，共用 HTML 模板与六个内容片段在构建时生成完整文档。

发布白名单仅含新站素材、旧桌面、CNAME、许可、robots 与 sitemap。旧场景模型、声音、视频、Draco、Three.js 等不再进入发布产物。仓库里的原实现与依赖是历史保留，不代表主页继续运行它们。

当前主站共用 JPEG 约 187 KiB，初版构建样式约 9 KiB、脚本不足 1 KiB；完整发布目录（含旧桌面）约 400 KiB。每次以实际构建结果为准。

## 验收要求

1. `npm run build` 和 `npm test` 通过。
2. 桌面与窄屏检查首屏、三个物件及关于页；内容无横向溢出。
3. 实际点击进入收藏、刷新、前进后退和回首页。
4. 明暗选择跨页、刷新与历史恢复一致；禁用脚本时可阅读。
5. 键盘跳过导航、链接激活、可见焦点；200% 文字仍能操作。
6. 线上独立路由与资产返回成功，控制台无阻断错误，新主页不请求旧动画资产。

## 内容边界

正文来自旧站公开自述与用户认可样稿，不把旧年龄、每日状态、项目行数、测试数量或计划当成实时事实。概念图已在关于页、详情说明和图片替代文字中标明。没有添加虚构的旅行照片、真实实物照片或未经提供的联系方式。

详见 [设计与素材说明](docs/collection-design.md)。
