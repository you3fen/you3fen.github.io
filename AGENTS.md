# 仓库协作约定

- 本仓库由用户本人维护，日常发布目标仅限 `origin`；`upstream` 属于外部仓库，不得向其推送、创建 PR、评论或请求评审，除非用户明确授权。
- 对修改类任务，达到用户要求的最终状态并完成相关验证后，只暂存本次任务涉及的文件，创建本地提交并正常推送当前分支到 `origin`；无需为 commit 或 push 再次询问。
- 检查、诊断、审查、解释等只读任务不创建提交，也不推送。
- 保留工作区中已有和无关的改动；无法可靠隔离本次提交范围时，停止在提交前并说明原因。
- 若测试失败、远端出现分叉、当前分支或发布目标不明确，停止在对应动作前并报告证据。
- 不得强推，不得改写已发布历史，除非用户明确授权。
